# shouldntComponentUpdate
Little app for testing assumptions with React's shouldComponentUpdate


### Install
`npm install` - or - `yarn install`


### Compile
`npm run dev` - or - `yarn dev`

Open index.html file in browser
